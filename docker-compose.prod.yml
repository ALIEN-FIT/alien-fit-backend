services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    environment:
      NODE_ENV: production
      PORT: 3000
    ports:
      - "${PROD_PORT:-3001}:3000"
